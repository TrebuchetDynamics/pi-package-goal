---
name: wayfinder-next
description: Select and complete one autonomous-ready frontier issue from an existing Wayfinder map. Use when the user invokes /skill:wayfinder-next with a named map; not for map creation, HITL decisions, or batch execution.
disable-model-invocation: true
---

# Wayfinder Next

Resolve exactly one existing autonomous ticket from a Wayfinder map, then stop. Read [Wayfinder](../wayfinder/SKILL.md) for the map model and naming rules.

## Entry and permission

The user must invoke `/skill:wayfinder-next <map URL or unambiguous map name>` against a named tracker. An unambiguous active map from the immediately preceding context is acceptable; never revive a stale map.

That invocation authorizes read/query operations plus these mutations for one existing ticket only:

- assign the selected ticket to claim it;
- post its progress or resolution comment;
- close it when its answer is complete and durable;
- append one linked decision gist to the existing map.

It does not authorize new issue creation, label or dependency changes, branch creation, commit/push, deployment, purchases, secrets, or destructive actions. If the tracker, map, assignee identity, or mutation mechanism is unavailable, ask one focused setup question and stop.

## Select

1. Load the map's Destination and Notes, then query its open child tickets. Do not load every closed ticket.
2. Compute the frontier: open children whose blockers are all closed and which have no assignee.
3. Keep only autonomous-ready tickets:
   - `research` tickets are eligible;
   - `task` tickets are eligible only when explicitly marked AFK and executable with current approved tools;
   - prototype, grilling, ambiguous, credential-gated, and all other HITL tickets are ineligible. **HITL tickets are ineligible even if the agent can guess an answer.**
4. Pick the first eligible unclaimed AFK frontier ticket using tracker/map order, then oldest creation time as the tie-breaker.
5. Read that ticket fully and verify it still fits the Destination. Refresh its assignment and blocker state immediately before claiming it.
6. Claim it. If another worker won the race, recompute once and choose the next eligible ticket; a second race stops the run.

Selection is complete only when one ticket is assigned to the current worker or no eligible ticket exists.

## Resolve

Work only the selected ticket's Question:

- Research: use `research-forge`; cite durable sources in the resolution.
- AFK task: route to the single narrow specialist that matches the task and use its normal validation.
- Repository changes: inspect Git state first, preserve unrelated work, and validate locally. If the ticket requires committed or pushed work, leave it open with a progress comment because this skill has no delivery permission.

Do not broaden into implementation of the map's Destination. Do not resolve a second ticket. Newly discovered decisions are follow-up drafts, not permission to create issues.

## Record

Use `triage` for approved tracker operations and prefix every tracker comment with:

```markdown
> *This was generated by AI during triage.*
```

When the answer is complete and durable:

1. Post the answer as the selected ticket's resolution comment, including evidence and links.
2. Close the selected ticket.
3. Append one line to the map's Decisions so far: `[ticket name](https://tracker.example/ticket) — one-line gist`.
4. Query the new frontier read-only, then stop.

When blocked or incomplete, post one progress comment only when it adds durable evidence, leave the ticket open and assigned, and report the exact owner action needed. Never close on partial work.

## Output contract

```text
Wayfinder next:
- map: <name + URL>
- selected: <ticket name + URL, type, selection reason>
- claim: <assigned|race-lost|blocked>
- work: <specialist and evidence>
- resolution: <closed|left open + reason>
- map update: <linked gist|none>
- surfaced follow-ups: <draft names only|none>
- next frontier: <read-only summary>
- repository state: <clean|changed paths, never shipped implicitly>
```

## Example

User: `/skill:wayfinder-next https://github.com/acme/project/issues/40`

Agent: loads map "Choose the storage migration", skips its grilling ticket, claims the oldest unblocked research ticket, resolves it with `research-forge`, posts evidence, closes that ticket, adds its linked gist to the map, reports the new frontier, and stops.

## Shared contract

Follow [the shared skill contract](../../shared/COMMON-CONTRACT.md) for repository study, tracker approval, artifact continuity, verification, and delivery boundaries.
