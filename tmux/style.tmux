# Local tmux style overrides.
# Installed to ~/.tmux/style.tmux only when missing, so each machine can keep
# its own colors while sharing the same ~/.tmux.conf.

# Session name + hostname block colors.
set -g @primary_color '#06d6a0'
set -g @primary_text_color '#101820'
